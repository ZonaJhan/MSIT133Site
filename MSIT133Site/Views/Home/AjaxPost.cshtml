
@{
    ViewData["Title"] = "AjaxPost";
}

<h1>AjaxPost</h1>
<div class="row">
    <div class="col-6">
        <form method="post">
            <div class="form-group">
                <label for="name">姓名</label>
                <input type="text" class="form-control" id="name" name="UserName">
            </div>
            <div class="form-group">
                <label for="email">電子郵件</label>
                <input type="email" class="form-control" id="email" name="Email">
            </div>
            <div class="form-group">
                <label for="age">年紀</label>
                <input type="number" class="form-control" id="age" name="Age">
            </div>
            <div class="form-group form-check">
                <input type="checkbox" class="form-check-input" id="exampleCheck1">
                <label class="form-check-label" for="exampleCheck1">Check me out</label>
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        @*<button type="reset" class="btn btn-primary">Clear</button>
            <button type="button" class="btn btn-primary">Button</button>*@
            <input type="button" class="btn btn-primary" value="Click" />
        </form>
        <div class="alert alert-primary" role="alert" id="divresult">
            @*@ViewBag.name*@
        </div>
    </div>
</div>
@*<div class="row">
        <div class="col-6">
            <form>
                <div class="form-group">
                    <label for="name">Name</label>
                    <input type="text" class="form-control" id="name">
                </div>
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" class="form-control" id="email">
                </div>
                <div class="form-group">
                    <label for="age">Age</label>
                    <input type="number" class="form-check-input" id="age">
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>

        </div>
    </div>*@

@section Scripts{ 
    <script>
    /*const btn = document.querySelector("css selectors")*/
    const btn = document.querySelector('button[type="submit"]')
    const divMessage = document.querySelector("#divresult")
    const txtName = document.querySelector("#name")
    const txtEmail = document.querySelector("#email")
    const txtAge = document.querySelector("#age")
    btn.addEventListener("click", (cancel) => {
        cancel.preventDefault();//cancel default action
        //console.log("button click")
        //ajax
        //step1
        const xml = new XMLHttpRequest();

        //step3
        xml.addEventListener("load", () => {
            const data = xml.responseText;
            divMessage.textContent = data;
        })

        //step2
        @*//get ?key=value

        const params = `UserName=${txtName.value}&email=${txtEmail.value}&age=${txtAge.value}`
        //post sent ("key=value&key=value")
        xml.open("post", "@Url.Content("~/api/register")");
        xml.setRequestHeader("Content-Type","application/x-www-form-urlencoded");
        xml.sent(params);*@
        //step 2
            const params = `UserName=${txtName.value}&age=${txtAge.value}&email=${txtEmail.value}`
            xml.open("post", "@Url.Content("~/api/Register")");
        xml.setRequestHeader("Content-Type","application/x-www-form-urlencoded")
        xml.send(params);
    })
    </script>

}


